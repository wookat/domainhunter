# R574 deployment follow-up (3bc9c841)

Setup complete: public production HTTP200, existing session Chrome CDP29229, existing Playwright and ffmpeg; no credentials needed. Lead owns storage restoration. Source inspected on origin/deploy/r192-r195 (checkout older): domain-row.tsx:222–258,355–370 label/expiry branches; home-page.tsx:843,866 integration labels; advanced-page.tsx:100–145,214–230 progress; monitors-page.tsx:36,250–257,301 control sizing; lib/persist.ts:SavedSearch/loadSearch and skill Results All filter. All access and state-trigger questions answered. Results replay of real rows explicitly authorized.

Safety: before navigation, persistent observer for all API requests, abort AI/check/webhook-test/share/write-monitor and fifth search if attempted; count attempts as failure. Never click recheck, Send test, registrar, stars, monitor controls, or AI. Maximum four POST search requests.

1. Navigate fresh /?q=chaxiang&lang=zh (search1). Measure [data-recheck] button rect, direct label span class/text/computed display, icon, aria-label/title for each home taken chip in zh/en at1280/375 using header language toggle. Require block at1280, none at375, nonempty aria, icon present. Screenshot chips in viewport, light plus375 dark. No recheck click.
2. Navigate /advanced?lang=zh, paste chaxiang.com nic.cn google.de google.com and click Check4 (search2). For each real taken row measure label as item1 and expiry [data-expiry=unknown] text/title/rect. Require nic.cn/google.de missing expiresAt to carry localized pending chip; google.com with expiresAt must not. Distinguish DOM presence from actual visible width/pixels; 0-width mobile chip is not visible PASS. Capture zh/en×1280/375 and375 dark.
3. Paste earlier exact20 domains, run once zh (search3) and once en (search4). Before each click install MutationObserver on status/progressbar changes and requestAnimationFrame state recorder with UTC/performance timestamps. Throttle genuine network to expose partial states (no mocked API). Capture running and completed at1280/375. Require monotonic now ending20; no frame /核验中 20\/20|Checking 20\/20/; first now20 status already completed and no spinner. Save full sequence and mutation records. Inspect any retryable-unknown recheck row in both languages/widths as item1. Optional375 dark.
4. Replay the real four-domain response plus observed real unknown row through dh:lastSearch:v1 (no fabricated status/expiry), navigate bare / and click All filter. Label all evidence "replayed snapshot of real rows". Repeat label/expiry matrix; no search or recheck. Remove audit snapshot afterward.
5. Navigate /monitors, save https://example.invalid/r575 via UI (no Send test). Measure configured test/edit/clear, then click Edit and measure input/save/test/cancel at zh/en×1280/375. Require all computed/rect heights40 desktop and44 mobile; screenshots light and375 dark. Cancel edit then two-step Clear. Require unconfigured badge and empty input.
6. Supported Mozilla-UA POST /api/monitor/list with fixture domains must return entries=[]; inspect no local monitors/shares created, no webhook. Sum exact API counts: AI0/check0/search≤4/webhook-test0; save timestamps/assets version note. Stop recording/driver and release Chrome to lead for storage restore. No source changes.
