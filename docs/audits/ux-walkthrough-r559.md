# R559 · 中文创业者零 AI 体验复走（R548/R549 修复后，version 7b7c18d1）

- 生产：https://hunt.zalize.com ，`/api/stats` version `7b7c18d1`（= 代码 `c85ca2c`，基线 `deploy/r192-r195` tip `a1f0851`）
- 时间：2026-09-06 17:40Z–18:07Z（浏览器实操 + curl/RDAP 复核）
- 方法：复用 R545 三个 persona 与**相同输入**，只走零 AI 路径；桌面 1600×1069 + 375×812（CDP 仿真）+ 深/浅色；录屏 `r559-ux`（80 s 精剪版随 PR 会话附件）
- 证据目录：`docs/audits/screenshots-r559/`（48 张，编号即下文 `NN`）、`docs/audits/r559/`（请求日志、usage 前后、storage 前/中/后、RDAP/WHOIS、/vs 抓取、CSV、探针）
- 标注约定：**验证过** = 本轮一手证据（截图/日志/curl/RDAP）；**未验证** = 未做或无法做；**推断** = 基于证据的判断/建议

## 0. 先看这里（P0/P1 摘要）

- **P0：无。P1：无。** R545 的两个 P1 均确认修复（§2），本轮新发现 3 个 P2、6 个 P3（§5）。
- **R548（P1-1）已注册行不再像可买**：`google.com`、`baidu.com.cn`、`stackpilot.app`（2026-07-01 到期、RDAP `redemption period`）在 **结果 chips / 候选清单 / 分享页 / 批量结果 / CSV** 五处都只显示「已注册 · 到期日 · —」，无首年价、无「去注册」；「批量去注册（N）」只计可注册行（清单 9 项 → `批量去注册（5）`；分享页 `复制 5 个可注册`）。用户视角：「已注册的就是已注册，不会再把它当 80 块的域名放进预算。」（`19`、`22`、`24`、`47`、`bulk-export.csv`）
- **R549（P1-2）/vs 正文价与表一致**：6 页 × zh/en 共 12 页，正文出现的每个价格都能在同页表格找到同值（§10.2）。用户视角：「读到哪都是同一个数，不用猜信哪个。」
- **开监控走得通且只要 1 步**：结果 chip 或批量结果行的「监控释放」一键 → `监控中 ✓`（POST `/api/monitor` 200），无二次确认，比 R545 的「加 → 确认」少 1 步；`/monitors` 立即出现该域名与到期日（`06`、`21`、`25`）。

## 1. 结论（SOP-04）

1. **两个 P1 修复在真实用户路径上成立**（验证过）：三个 persona 在桌面/375、深/浅色都没有再遇到「已注册域名配普通价 + 去注册」或「同页两套价格」；`/s/:id` 与批量 CSV 也一致。
2. **捡漏路径可用但「收不到通知」**（验证过）：开监控 1 步；但 `/monitors` 页只有「取消监控 / 查可用性 / 立即刷新」，正文写「推送 webhook 通知」却**没有任何输入口**；webhook 输入实际藏在 `/shortlist` 的「监控动态」折叠面板里（需展开才出现）。想捡漏的用户开完监控后必须自己定时回来看（P2-1）。
3. **`/monitors → 查可用性` 落地首页后「AI 猎名」tab 仍高亮、主按钮是「开始猎取」**（验证过）：实际已自动跑零 AI 精确核验并出结果，但结果在主按钮和 AI 引导之下（桌面 y≈966，375 需滚约两屏）；用户很容易顺手点「开始猎取」花掉 AI 次数（P2-2）。
4. **零 AI 证明**（验证过）：`/api/usage?days=1` 前后 `searches 0→0 / fast 0→0 / refine 0→0`；请求日志 104 条 API 请求中 `/api/ai-search` 0 条（§3）。
5. **storage 字节级还原**（验证过）：还原后 dump SHA-256 与备份一致 `61c02c9b…2081`（§4）。
6. **375px 20 页（10 页 × 深/浅）`scrollWidth == clientWidth == 375`，无横向溢出；Tab 26 步可达「监控释放」并有可见焦点环**（验证过，§9.2、`48`）。
7. **Nielsen 0–4 均分 2.5 → 2.8**（§9.1）：一致性 1→3、匹配真实世界 2→3、防错 2→3 是升幅；「帮助识别错误」3→2（`.ai` 上游限流暴露的「未知」态无原因/重试）。

## 2. R548/R549 重点复验（任务 2）

### 2.1 已注册域名在四个面上的呈现（验证过）

| 域名 | 面 | 显示 | 有价？ | CTA | 证据 |
|---|---|---|---|---|---|
| `google.com` | `/advanced` 批量「其余候选」 | ~~google.com~~ 已注册 2028-09-14 到期 | 否 | 仅「收藏」 | `19`、`20` |
| `google.com` | CSV | `google.com,taken,…,,"2028-09-14"`（price 列空） | 否 | — | `r559/bulk-export.csv` |
| `baidu.com.cn` | `/advanced` 批量 | ~~baidu.com.cn~~ 已注册 2029-02-15 到期 | 否 | 仅「收藏」 | `19`、`21` |
| `stackpilot.app`（近期到期） | 批量 → 清单 → 分享 → 首页 chip | 已注册 2026-07-01 到期 + `监控释放`/`监控中 ✓`；清单评分/价格列全「—」；分享页 `已注册 — —`；chip 无价 | 否 | 开监控 | `20`、`21`、`22`、`24`、`47` |
| `chaxiang.com/.cn/.cc` | 首页 chip → 清单 → 分享 | 已注册 + 到期日；清单「开监控」/「监控中 ✓」；分享页 `已注册 — —` | 否 | 开监控 | `02`、`06`、`07`、`22`、`24` |
| 可注册对照 `lingxicha.com` 等 | 同上四面 | `首年 $11.08 ≈¥80` + 去注册；CSV price 列有值 | 是 | 去注册 | `19`、`22`、`24` |

- 「批量去注册（5）」= 清单 9 项中 5 个可注册（`22`）；分享页 `复制 5 个可注册`、`去注册` 只在 5 个可注册行（`24`）。R545 P1-1「Register all (5) 把 taken 计入」不再复现。
- 用户视角一句话（A/C）：「灰掉、划线、只有到期日和小铃铛——一眼知道这是别人的，能做的只是盯着它掉。」
- **未验证**：`/s/:id` 由未登录第三方在真机打开的观感（本轮同浏览器打开）；aftermarket/premium 语义仍无（与 R545 相同，见 P3-6）。

### 2.2 `/vs` 正文 vs 同页表（验证过，curl Mozilla UA，`r559/vs-check.json`）

| 页 | 语言 | 正文价格 | 同页表格 | 一致 |
|---|---|---|---|---|
| de-vs-com | zh / en | ≈$3 / 21 元；$4 / 29 元 | `.de $2.9 ¥21 · 续 $4.07 ¥29` | ✔（约数口径） |
| com-vs-io | zh / en | 202/373 元；80/80 元；十年差 2759 元 | `.io $28.12 ¥202 · $51.8 ¥373`；`.com $11.08 ¥80 · ¥80` | ✔ |
| win-vs-vip | zh / en | ≈$5/33 元、$6/41 元；$4/30 元、$5/37 元 | `.win $4.61 ¥33 · $5.64 ¥41`；`.vip $4.12 ¥30 · $5.15 ¥37` | ✔ |
| com-vs-cn | zh / en | 33/38 元；80/80 元 | `.cn 参考 ≈$5 ¥33 · ≈$5 ¥38`；`.com ¥80 · ¥80` | ✔ |
| io-vs-ai | zh / en | 202/373 元；595/595 元 | `.io ¥202 · ¥373`；`.ai $82.7 ¥595 · ¥595` | ✔ |
| com-vs-ai | zh / en | 595/595 元；80/80 元 | `.ai ¥595 · ¥595`；`.com ¥80 · ¥80` | ✔ |

R545 P1-2（导语「.io 首年 259 元 / .ai 499 元」与表 202/595 矛盾）不再出现；12 页均无 `/api/ai-search` 链接。用户不会再读到矛盾价。

### 2.3 开监控步数（验证过）

| 起点 | 步数 | 结果 | 证据 |
|---|---|---|---|
| 首页 chip `chaxiang.cc`（已注册 2026-11-13） | 1（点「监控释放」） | `监控中 ✓`，POST `/api/monitor {"domain":"chaxiang.cc","enabled":true}` 200 | `06`、`net.log` 17:47:27 |
| `/advanced` 批量行 `stackpilot.app` | 1 | `监控中 ✓` + `/monitors` 图标链接，清单 8→9 | `20`→`21`、`net.log` 17:59:21 |
| 清单已注册行 | 1（「开监控」） | 同上 | `22` |
| 设置通知 | 3（`/shortlist` → 展开「监控动态」→ 填 webhook → 保存） | 入口不可发现（P2-1） | `26` |

R545 为「加 → 5 s 二次确认」两步（撤销仍保留二次确认，`/monitors` 取消监控 → 确认，验证过）。

## 3. 零 AI 证明（验证过）

- `r559/usage-before.json`（17:40Z）→ `r559/usage-after.json`（18:07Z，最后一次 API 请求 18:06:34 后 ≥ 60 s）：`searches 0→0`、`fast 0→0`、`refine 0→0`；`outbound.porkbun 38→44`、`aliyun 0→4`（本轮点击注册商菜单/批量去注册产生，未付款）。
- `r559/net.log`（CDP 网络拦截，全程）：104 条 API 请求 = `/api/registrars 39`、`/api/prices 30`、`/api/search 9`、`/api/stats 7`、`/api/monitor/list 4`、`/api/monitor 4`、`/api/share/:id GET 3 / DELETE 2`、`/api/share 2`、`/api/click 2`、`/api/sync 1`、`/api/monitor/changes 1`；**`/api/ai-search 0`**（`grep -c ai-search net.log` = 0）。
- 未点击任何 AI CTA / 示例 / refine / 「再来一轮」；`?q=` 落地页只发 `/api/search`（18:02:17、18:04:01，`net.log`）。

## 4. storage 还原证明（验证过）

- 备份 `r559/storage-before.json`：`domainhunter:lang=en`、`domainhunter:shortlist=[]`、`domainhunter:theme=dark`，sessionStorage 空。
- 测试中新增 `dh:myShares:v1`、`domainhunter:monitor`、`domainhunter:sync:code`（已脱敏），lang→zh、theme→light（`r559/storage-during-test.json`）。
- 先清理服务端痕迹：取消 2 个监控（`/monitors` 名额 4/500 → 2/500）、删除 2 个分享（`GET /api/share/mPf73SyTHC`、`/LYpSNRZ2gH` → 410）；再 `restore_storage.py` 覆写并二次 dump。
- `r559/storage-sha256.txt`：before 与 after-verify 均 `61c02c9bb341fc6f12dea8109df524fe3287096c826250372f657de2072dc081`。
- 未验证：服务端同步码对应的清单快照仍存在（90 天有效），无删除接口。

## 5. 问题清单（P0–P3）

> 每条：现象 → 复现 → 影响 persona → 用户原话 → 证据 → 建议（只建议不改码）。

### P0 / P1

无。

### P2

<a id="p2-1"></a>**P2-1 想捡漏的用户找不到「通知我」在哪**（验证过）
- 现象：`/monitors` 正文「开了监控的域名每 6 小时自动复查，掉落/被注册会记录在监控动态并推送 webhook 通知」，但页面上 `input/textarea/select` 数量为 0，按钮只有「立即刷新状态 / 取消监控」，链接只有「查可用性」。webhook 输入框在 `/shortlist` →「监控动态」折叠面板内（默认收起，`26`），placeholder「https://… 通知 webhook（可选）」，说明「填入飞书/Slack/自建服务的 webhook 地址」。POST `/api/monitor` 默认 `"webhook":""`。
- 复现：批量结果点「监控释放」→ 点旁边 `/monitors` 图标 → 找通知设置。
- 影响：C（捡漏）、A（`chaxiang.cc` 2026-11-13 到期）。
- 用户原话：「说会推送通知，推到哪？我连个填邮箱的地方都没看到。」
- 证据：`25`、`26`、`31`、`41`；`net.log` 17:59:21 `"webhook":""`。
- 建议（推断）：`/monitors` 顶部放同一 webhook 输入；开监控成功 toast 加「设置通知 →」；非技术用户加邮件/微信选项（需产品决策，本轮不改）。

<a id="p2-2"></a>**P2-2 `?q=` 落地页已跑精确核验，但「AI 猎名」tab 高亮、主 CTA 为「开始猎取」**（验证过）
- 现象：`/monitors`「查可用性」→ `/?q=stackpilot.app`：`aria-selected` 为 `AI 猎名:true / 精确核验:false`，主按钮「开始猎取」(⌘ Enter)，其下小字「看起来是现成的名字？可以直接核验，不消耗 AI 次数：直接核验 stackpilot.*」，再往下才是 9 个结果（首个结果桌面 y≈966；375 在两屏之后）。Tab 顺序「开始猎取」在「直接核验」之前。
- 复现：任一 `?q=<domain>` 入口（/monitors、分享页「我也要猎名」除外）。
- 影响：C/B（想复查某域名，误点 AI 花次数）；A（375 看不到结果以为没查）。
- 用户原话：「我是来看它掉没掉的，怎么又让我『开始猎取』？结果在哪？」
- 证据：`47`、`28`、`38`；`net.log` 18:04:01 只发 `/api/search`。
- 建议（推断）：`?q=` 带域名/纯拼音时自动选中「精确核验」tab 并滚到结果；或 `/monitors` 的「查可用性」直接锚到结果区。

<a id="p2-3"></a>**P2-3 批量 `.ai` 全部「未知」，无原因、无重试入口**（验证过：现象；原因部分验证）
- 现象：28 项中 5 个 `.ai` 全为「未知」黄标（`19`），行内只有「复制/收藏」，没有 R548 期望的「重新核验」按钮（清单页有「重新核验」整体按钮，批量结果页无）。直接 `POST /api/search {"roots":["lingxicha"],"tlds":["ai"]}` 返回 `{"status":"unknown","method":"rdap","detail":"http-429"}`——上游 `.ai` RDAP 限流。
- 影响：B（出海 SaaS 要 `.ai`）、C。
- 用户原话：「未知是什么意思？能注册还是不能？我要再点哪里？」
- 证据：`19`、`bulk-export.csv`（5 行 `unknown`）、`net.log` 17:55 `/api/search`。
- 建议（推断）：未知行显示原因（「注册局限流，稍后自动重试」）+ 单行「重新核验」；CSV `status` 加 `detail` 列。

### P3

- **P3-1 `/prices` 子串筛选未把精确后缀置顶**（验证过，R545 P3-4 仍在）：筛 `io` 14 条、`.ai` 7 条且 `.ai` 不在首位；`12`、`13`。建议精确匹配置顶。
- **P3-2 `/advanced` 命名断裂 + 粘贴区在组合器下**（验证过，R545 P2-4 仍在，本轮降级为 P3 因文案已写「不消耗 AI 次数 · 一次最多 200 个」且桌面一屏可见）：首页 tab「批量核验」→ 页标题「高级模式」；375 粘贴框在第二屏（`18`、`32`、`42`）。
- **P3-3 批量核验无 x/28 进度**（部分验证：完成态无「已核验 28/28」，中间态未专门截图）：只有「已识别 28 个域名」与流式增长；`19`。R545 P2-5 仍在。
- **P3-4 CSV 价格列为带标签字符串**（验证过）：`first_year_price` = `"首年 $28.12 ≈¥202"`，表格软件无法求和；已注册行 `expires_at` 有值、price 空（正确）。`bulk-export.csv`、`shortlist-export.csv`。建议拆 `price_usd / price_cny` 数值列。
- **P3-5 分享页不带备注与到期日**（验证过）：`/s/:id` 已注册行只有「已注册 — —」，合伙人看不到「2026-07-01 到期 / 监控中」，也看不到备注（备注不外发是 R545 记录的隐私设计，但到期日是公开事实）；`24`、`30`、`40`。
- **P3-6 仍无 aftermarket/premium 语义**（验证过，R545 记录）：三态「可注册/已注册/未知」；已注册行现在不再给价，误导已消除，但「可买二级市场」信息也没有——记录供产品判断。
- **P3-7 375 `/shortlist` 域名表在「分享链接 + 同步」两个区块之下**（验证过，R545 C-375 记录仍在）：首屏只见按钮与面板，`29`、`39`。
- **P3-8 `stackpilot.dev` 首页 chip「已注册」无到期日，而 RDAP 返回 `2027-04-06`**（验证过现象，原因未验证）：`47`；同页其它 `.app/.co/.net` 都有到期日。可能是解析 `.dev` RDAP `events` 字段失败或缓存旧值——需代码侧确认。

## 6. 亮点（验证过）

- 已注册行在**五个面**（chip/清单/分享/批量/CSV）统一为「划线 + 已注册 + 到期日 + —」，可注册行才有价与 CTA；「批量去注册（N）」与分享页 `复制 N 个可注册` 数字一致（`19`、`22`、`24`）。
- 近期到期/赎回期域名（`stackpilot.app` 2026-07-01、`stackpilot.net` 2026-11-30、`chaxiang.cc` 2026-11-13）自动带「监控释放」按钮，一键即监控，`/monitors` 即时出现（`06`、`21`、`25`、`47`）。
- `/vs` 12 页正文与表同源；zh 用「202 元」、en 用「¥202」，口径一致（§2.2）。
- `/tld/io`、`/tld/ai`、`/prices` 首年/续费并列并具名 Porkbun 与拉取时间（`11`、`15`、`16`）。
- 375 深/浅色 20 页零横向溢出；浅色下已注册划线、黄字到期、绿 `监控中 ✓`、焦点环均清晰（`37`–`46`、`48`）。
- 键盘：从输入框 Tab 26 步可达最后一行「监控释放」，全程 `outline/box-shadow` 可见；已注册行没有「死」的去注册按钮占位（`48`）。
- 破坏性操作（取消监控、删除分享）仍有二次确认；分享删除后 `GET /api/share/:id` → 410 真正失效。

## 7. Persona 逐段摘要

### A · 茶叶电商（zh，`chaxiang`）
| 视口 | 期望 vs 实际 | 困惑点 | 信任点 | 证据 |
|---|---|---|---|---|
| 桌面 | 精确核验 9 后缀：`.com/.cn` 已注册带到期日，`.io/.tech` 可注册 ✔；24 变体 ✔；「查更多后缀」409 项 53 可注册 / 10 已注册 / **346 未知**（`.ai` 等上游限流）✘；星标→清单：已注册行「—」+「开监控」✔；备注/排序/分享/CSV/去注册（Porkbun 预填，未付款）✔；`chaxiang.cc` 监控 1 步 ✔ | 346 个未知无解释（P2-3 同源）；`/tld/cn` 仍为静态参考价 | 已注册行与注册局到期日一致到天（§10.1）；「本地保存 · 注册前建议重新核验」 | `01`–`09` |
| 375 深/浅 | 首屏精确 tab 可见；结果 chips 单列；无溢出 ✔ | `?q=` 落地 AI tab 高亮（P2-2） | 同桌面 | `27`、`28`、`37`、`38` |

### B · 出海 SaaS（en，`stackpilot`）
| 视口 | 期望 vs 实际 | 困惑点 | 信任点 | 证据 |
|---|---|---|---|---|
| 桌面 | `.com/.io/.ai/.app/.dev/.co/.net/.me` Taken + 到期日，`.cn` Available ≈$5 ✔；`/prices` 三价可查 ✔ 但筛选过宽（P3-1）；`/vs/com-vs-io` 等 6 页正文=表 ✔；`/tld/io` `$28.12/$51.8`、`/tld/ai` `$82.7/$82.7` 与 `/prices`、`/vs` 同值 ✔；`/why` 说清定位 ✔ | `.ai` 两年起注仍无提示（R545 P2-2，本轮 curl `/tld/ai` zh/en 均无「minimum term/2-year/两年」——验证过，未修） | 「Live prices from Porkbun (fetched …)」 | `10`–`17` |
| 375 | `/prices`、`/vs`、`/tld` 表换行不横溢 ✔ | — | 同桌面 | `33`–`35`、`43`–`45` |

### C · 批量 5 候选（zh）
| 视口 | 期望 vs 实际 | 困惑点 | 信任点 | 证据 |
|---|---|---|---|---|
| 桌面 | `lingxicha/yunqiji/shanhetea/mochamo/qingfengke × com,cn,net,io,ai` + `google.com/baidu.com.cn/stackpilot.app` = 28 项：15 可注册 / 8 已注册 / 5 未知 ✔；CSV 28 行 ✔；星标 3 → 清单 9，`批量去注册（5）` ✔；排序 到期日/首年价/域名 三种均正确重排 ✔；分享 `/s/LYpSNRZ2gH` 已注册行无 CTA ✔；同步码 8 位 ✔；`stackpilot.app` 监控 1 步 → `/monitors` 2 个 ✔；取消监控/删除分享二次确认 ✔ | `.ai` 5 个未知无解释（P2-3）；`/monitors` 找不到通知设置（P2-1）；「查可用性」落地 AI tab（P2-2） | 「不消耗 AI 次数 · 一次最多 200 个」；已注册行划线 + 到期日 | `18`–`26`、`47`、`48` |
| 375 深/浅 | 批量/清单/分享/监控页无溢出 ✔；清单表在分享/同步面板之下（P3-7） | 粘贴框第二屏（P3-2） | 同桌面 | `29`–`32`、`39`–`42` |

## 8. 性能

未验证（本轮未采 Performance API；R545 全部 < 500 ms，本轮主观无明显退化）。

## 9. 打分

### 9.1 Nielsen 十原则（0–4；R545 原表为 1–5，此处按 −1 换算列出以便同尺度对比）

| # | 原则 | R545 | R559 | 变化 | 证据 |
|---|---|---|---|---|---|
| 1 | 系统状态可见性 | 2 | 2 | = | 「全部 9 / 可注册 1 / 已注册 8」计数 ✔；批量仍无 x/28（P3-3）；346/5 个「未知」无原因（P2-3） |
| 2 | 系统与真实世界匹配 | 2 | 3 | +1 | 已注册不再配价与去注册（§2.1）；「高级模式」偏技术仍在（P3-2） |
| 3 | 用户控制与自由 | 3 | 3 | = | 取消监控/删分享二次确认、分享 410 真失效 ✔；同步码快照无法撤回 |
| 4 | 一致性与标准 | 1 | 3 | +2 | 同页价一致（§2.2）；五个面已注册呈现一致（§2.1）；仍有首页 chip `$` vs 清单 `≈¥`（R545 P3-7）、`?q=` tab 状态不一致（P2-2） |
| 5 | 防错 | 2 | 3 | +1 | 「批量去注册（N）」只计可注册 ✔；`?q=` 落地主 CTA 为 AI（P2-2）；`.ai` 两年起注仍无提示 |
| 6 | 识别而非回忆 | 3 | 3 | = | 排序/筛选/监控状态可见 ✔；webhook 入口需记住在清单折叠面板（P2-1） |
| 7 | 灵活与效率 | 3 | 3 | = | 一键监控、批量 200、CSV、同步码 ✔；`/prices` 筛选（P3-1）、CSV 价格非数值（P3-4） |
| 8 | 美学与极简 | 3 | 3 | = | 深/浅色一致、已注册行降噪 ✔；375 清单表被面板推下（P3-7） |
| 9 | 帮助用户识别/诊断/恢复错误 | 3 | 2 | −1 | 「未知」无原因与单行重试（P2-3）；本轮 R545 未触发该态，现因 `.ai` 限流暴露 |
| 10 | 帮助与文档 | 3 | 3 | = | `/why`、`/tld/*`、监控说明 ✔；通知怎么设没有文档（P2-1） |

均分 **2.5 → 2.8**（十项算术平均：R545 25/10，R559 28/10）。两个 P1 修复直接拉高 #2/#4/#5；新暴露的「未知」错误态拉低 #9。

### 9.2 移动可用性（375×812 仿真，深/浅各 10 页）

| 项 | 分（0–4） | 证据 |
|---|---|---|
| 无横向溢出 | 4 | 20 个 probe `sw=cw=bsw=375`（`r559/probes.jsonl`，`27`–`46`） |
| 关键入口可达 | 3 | 精确 tab、监控、清单可点；`?q=` 结果两屏之下（P2-2）；粘贴框第二屏（P3-2） |
| 内容截断/可读 | 3 | 已注册划线 + 到期日在 375 完整；浅色对比清晰（`39`–`41`） |
| 触控目标 | 未验证 | 本轮未量命中区 |
| 浮层/菜单不越界 | 4 | 分享/同步/监控动态为页内区块（`29`、`39`） |
| 键盘可达 | 4 | 桌面 Tab 26 步到「监控释放」，焦点环可见（`48`）；375 软键盘未验证 |

## 10. 事实核验

### 10.1 状态/到期日 × 注册局（`r559/rdap-verification.jsonl`；IANA bootstrap，`.cn` 走 `whois.cnnic.cn`，`.io` 无 RDAP 走 `whois.nic.io`）

| 域名 | 站内显示 | 独立核对 | 一致 |
|---|---|---|---|
| `chaxiang.com` | 已注册 2027-05-13 | RDAP 200 `2027-05-13T18:26:54Z` 22net | ✔ |
| `chaxiang.cn` | 已注册 2026-12-11 | CNNIC WHOIS `2026-12-11` 阿里云 | ✔ |
| `chaxiang.cc` | 已注册 2026-11-13 | RDAP 200 `2026-11-13T09:03:01Z` | ✔ |
| `stackpilot.app` | 已注册 2026-07-01 + 监控释放 | RDAP 200 `2026-07-01T15:13:00Z`，status `pending delete / redemption period` | ✔（确为待掉落） |
| `google.com` | 已注册 2028-09-14 | RDAP 200 `2028-09-14T04:00:00Z` MarkMonitor | ✔ |
| `baidu.com.cn` | 已注册 2029-02-15 | CNNIC WHOIS `2029-02-15` | ✔ |
| `yunqiji.cn` | 已注册 2026-12-12 | CNNIC WHOIS `2026-12-12` | ✔ |
| `qingfengke.net` | 已注册 2032-09-23 | RDAP 200 `2032-09-23T06:22:47Z` | ✔ |
| `stackpilot.ai` | 已注册 2027-07-31 | RDAP 200 `2027-07-31T22:44:26Z` | ✔ |
| `stackpilot.dev` | 已注册（无到期日） | RDAP 200 `2027-04-06T10:59:44Z` | 状态 ✔，到期日缺（P3-8） |
| `lingxicha.com` | 可注册 $11.08 | RDAP 404 | ✔ |
| `stackpilot.cn` | 可注册 ≈$5 | CNNIC `No matching record` | ✔ |

12/12 状态一致，11/12 到期日一致到天；`lingxicha.ai` 等站内「未知」= 上游 RDAP 429（`net.log`），非误判。

### 10.2 价格

- `/prices`（408 后缀，Porkbun 实时）`.io $28.12/$51.8`、`.ai $82.7/$82.7` = `/tld/io`、`/tld/ai` = `/vs` 表 = 首页 chip/批量「首年 $28.12 ≈¥202」（验证过，`11`–`16`、`19`）。
- `.cn` 站内为「首年 ¥33」静态参考价（`/tld/cn`、批量、`/vs/com-vs-cn` 表标「参考」），未与注册商实时价核对（未验证，与 R545 P3-8 相同）。

## 11. 未验证 / 建议下一步

- 未验证：真机触控命中区、软键盘；LCP/FCP；第三方浏览器打开 `/s/:id`；`.cn` 实时价；`stackpilot.dev` 到期日缺失的代码原因；webhook 实际推送。
- 下一步（按短周期）：
  1. **P2-1** `/monitors` 加 webhook 输入 + 开监控成功后的「设置通知」引导（同时补 zh/en i18n）。
  2. **P2-2** `?q=` 落地自动选「精确核验」tab 并滚到结果；`/monitors` 「查可用性」锚到结果。
  3. **P2-3** 「未知」行显示原因 + 单行「重新核验」；批量结果页补 `重新核验未知（N）`。
  4. P3-8 排查 `.dev` RDAP 到期日解析；P3-4 CSV 拆数值列；P3-1 `/prices` 精确置顶。
  5. R545 遗留未修：`.ai` 两年起注提示、首页 `$` vs 清单 `≈¥`、`/advanced` 命名。

## 12. 需注意

- 本轮 0 次 `/api/ai-search`，0 次付款；生产 `outbound` 计数 +6 porkbun / +4 aliyun 来自本轮点击注册商菜单（测试噪声）。
- 已清理：2 个监控（名额回到 2/500）、2 个分享（410）；localStorage 已字节级还原；服务端同步码快照（90 天）无法删除。
- `net.log` 中分享删除 token 与同步码已脱敏为 `<redacted>`。
- 报告只新增 `docs/audits/ux-walkthrough-r559.md`、`docs/audits/r559/`、`docs/audits/screenshots-r559/`，不改任何代码/测试。
- Nielsen 分数为单人走查主观评分，趟数 1，仅用于同尺度前后对比。

## 附录 · 证据文件索引（`docs/audits/r559/`）

| 文件 | 内容 |
|---|---|
| `net.log` | CDP 全程请求/响应日志（NAV/REQ/RESP），`ai-search` 0 条 |
| `usage-before.json` / `usage-after.json` | `/api/usage?days=1` 前后 |
| `storage-before.json` / `storage-during-test.json` / `storage-after-verify.json` / `storage-sha256.txt` | storage 备份、测试中态、还原后 dump 与哈希 |
| `rdap-verification.jsonl` / `rdap.py` | 12 个域名独立核对与脚本 |
| `vs-check.json` / `vs-check.txt` / `vs_check.py` | 6 页 × zh/en `/vs` 抓取（Mozilla UA）与表/正文提取 |
| `probes.jsonl` / `m375.py` | 每张截图的 `scrollWidth/clientWidth/theme` 探针与 375 仿真脚本 |
| `shortlist-export.csv` / `bulk-export.csv` | 清单导出（5 行）与批量导出（28 行） |
| `recording-annotations.json` | 录屏 `r559-ux` 的标注时间线 |
| `../screenshots-r559/01–48` | 见各节编号 |
