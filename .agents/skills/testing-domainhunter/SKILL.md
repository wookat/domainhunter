---
name: testing-domainhunter
description: How to run zero-AI production audits of DomainHunter (hunt.zalize.com) without consuming AI quota, with storage backup/restore and MCP/API verification.
---

# Testing DomainHunter (hunt.zalize.com)

## Zero-AI constraint
- `/api/ai-search` consumes DeepSeek quota. Never click the AI CTA ("Start hunting"/开始寻找), example prompts, refine, or one-more-round buttons.
- Safe non-AI paths: quick-check (Exact check tab), bulk paste on `/advanced`, MCP JSON-RPC at `POST /mcp`, all static/hub/content pages.
- Prove zero AI calls by diffing `GET /api/usage` before/after (parsed JSON equality). Quick-check/bulk/MCP do not increment usage. Since R487 every `usage:{date}` counter (`searches`/`byTld`/`fast`/`refine`/`aiErrors`/`fallbacks`/`llmProvider`/`outbound`/`outboundByTld`/`shareWrite*`) is written to per-isolate shard keys `usage:{date}:<shard>` (1s in-isolate batching) and summed by `/api/usage`, exactly like `pv:*` — so the aggregate is exact across isolates, but freshly written shards can take up to ~60s to appear (KV eventual consistency): take the "before" snapshot, do the actions, then **wait ≥60s** before the "after" read, and hard-reload `/api/usage` (it is `max-age=300`). A `+0` read a few seconds after an `/api/click` is NOT a loss.

## Storage backup/restore
- User prefs live in localStorage: `domainhunter:lang`, `domainhunter:theme`, `domainhunter:shortlist`, `dh:onboardDismissed:v1`, `dh:lastSearch:v1`, `domainhunter:recent-searches`.
- Back up ALL keys before touching the site; restore byte-for-byte at the end and verify via re-dump diff. Helper scripts: `docs/qa/dump_storage.py` / `docs/qa/restore_storage.py` (Playwright over CDP, `connect_over_cdp("http://localhost:29229")` or whatever the session CDP port is).

## Gotchas
- Direct `curl`/urllib without a browser User-Agent may get 403 from Cloudflare on HTML pages; add a Mozilla UA.
- Raw `websocket-client` CDP connections need `suppress_origin=True` (Chrome rejects the localhost Origin otherwise).
- `/s/:id` share pages: since R510 the SSR shell mirrors `GET /api/share/:id` — revoked → HTTP 410, unknown/invalid id → HTTP 404, both with `<meta name="robots" content="noindex">` and a neutral bilingual `<title>` (「分享已撤销 / This share has been revoked」, 「分享不存在或已过期 / Share not found or expired」); live shares stay 200. The SPA still renders the same revoked/not-found UI. Revoke via `DELETE /api/share/:id` with `{"token": revokeToken}` from the create response.
- SPA 404 status: unknown top-level paths and unknown `/tld|/guide|/vs` slugs DO return real HTTP 404 with a branded page.
- MCP `tld_prices` returns `prices` as a dict keyed by TLD (not an array); expected tldCount = current /tld count (186 as of R301).
- Hub counts (as of R303): /tld 186, /guide 182, /vs 216; quick-check "All" = /tld count + 1 (com.cn). Authoritative counts live in `scripts/content-counts.json`.
- SSR language matrix (R492): `scripts/seo-audit/lang-matrix.sh [origin]` probes 9 SEO paths × {bare, `Accept-Language: en`, `?lang=en`} and tabulates `<html lang>` / canonical / hreflang / `Vary` / title language. Expected: every en body (incl. bare path + `Accept-Language: en`) has canonical `…?lang=en`, zh bodies have the bare canonical, every text/html response carries `Vary: Accept-Language` while `/api/*`, sitemap, assets have no Vary. `SEO_AUDIT_ORIGIN=http://localhost:8787 node scripts/seo-audit/fetch.mjs sample --out /tmp/x` + `analyze.mjs --out /tmp/x` runs the R488 canonical/hreflang audit against local wrangler.
- On /vs pages, en language is served via `?lang=en` query param (a `/en/...` path prefix 404s); the hub filter input (`input[type=search]`) is the fastest way to locate a specific compare card.
- Local testing without deploying: `pnpm install`, then in `apps/web` run `pnpm build` (vite) followed by `npx wrangler dev --port 8787` — it serves the built `dist` plus the worker routes (sitemap.xml, /llms.txt, /api/*) on localhost. `/prices` shows static reference prices when live Porkbun quotes are unavailable locally. Start it with `setsid nohup npx wrangler dev --port 8787 &` from `apps/web`: if launched from a shell that gets timeout-reaped, the dead instance's port can briefly keep returning 200 with an empty body.
- To count /prices table rows objectively, count `main a[href^="/?tld="]` (Hunt links) — the table is not a `<table>` element.
- 375px viewport checks: use CDP `Emulation.setDeviceMetricsOverride` in a separate tab; assert `document.documentElement.scrollWidth <= 375`.
- Lighthouse CLI with `--chrome-flags="--headless=new --no-sandbox"` works fine against production.
- Hub pages (/tld, /guide, /vs) have a sticky group-chips anchor nav (R415): chips are `nav[aria-label="分组导航"/"Group navigation"] a[href^="#hub-g-"]`, sections are `id="hub-g-<group>"` with `scroll-mt-28`; the back-to-top button (`button[aria-label="回到顶部"/"Back to top"]`, 44×44) appears once `scrollY > innerHeight`. Note the chips row can show a horizontal OS scrollbar that makes nav bottom (~124px) exceed scroll-mt-28 (112px), slightly clipping headings after anchor jumps — measure with getBoundingClientRect if verifying anchor offsets.
- Before browser-testing a freshly checked-out branch locally, confirm `apps/web/dist/assets` mtimes are newer than the checkout (wrangler dev serves the prebuilt dist; rerun `pnpm build` in apps/web if stale).
- When taking 375px CDP-emulated screenshots of a background tab, `page.screenshot()` can return an all-black image for the dark theme; use `Page.captureScreenshot` via the CDP session (and `bring_to_front`) instead.
- The /tld hub has an `input[type=search]` filter (aria-label "Filter TLDs…/筛选后缀…") — fastest way to locate a single TLD and its category.
- Results page without AI: the page restores from **sessionStorage** (not localStorage) key `dh:lastSearch:v1` (apps/web/src/lib/persist.ts). Seed it in the same tab (`values.description` non-empty, `values.style`/`lengthPref` = "", rows with label/domain/tld/status/score/scores/meaning) then navigate to `/`. Taken rows render a compact early-return row (no swatch/card) and are filtered out of grid view — use a `status: "unknown"` row to exercise the not-available (no green check) BrandCard path.
- Stale `wrangler dev` can hold port 8787 with an empty-body 200 (index bundle 404s). Check with `curl -s localhost:8787/ | grep -o 'assets/index-[^"]*'`; if empty, kill the workerd/wrangler pids shown by `ss -ltnp | grep 8787` and restart in a persistent tty shell (`exec tty=true, shell_id=...`) — `setsid nohup … &` from a one-shot exec gets reaped.
- To prove a layout issue is pre-existing, build the base commit in a worktree: `git worktree add /tmp/dh-base <sha>`, symlink `node_modules` (root and apps/web) from the main checkout, `npx vite build`, `npx wrangler dev --port 8788`, then run the same fixture against both ports.
- CDP `Emulation.setDeviceMetricsOverride` from a Playwright `new_cdp_session` persists after the session detaches; to return to desktop send `Emulation.clearDeviceMetricsOverride` from a NEW session and reload. Emulated 375 viewport in the visible Chrome shows a classic 15px scrollbar (clientWidth 360) — inject `::-webkit-scrollbar{display:none}` before measuring `scrollWidth` if you need true 375 numbers.
- 375px overflow triage: `main` is a column-flex item with `mx-auto`, so any child's min-content (e.g. the non-wrapping TLD chip strip) widens it unless `main` has `w-full` (fixed in R468). To find the culprit, force `main.style.width='300px'` and list descendants whose right edge exceeds it.
- Local `wrangler dev` CAN fetch live Porkbun quotes, so TLD detail pages show live prices rather than static reference prices; to verify `TLD_PRICES`, grep the SSR HTML for「静态参考价：首年 ¥X · 续费 ¥Y/年」.
- R463 起结果页 Space 是两步确认：焦点离开输入框首按只出提示「再按一次空格确认再来一轮」（3s 超时还原），3s 内第二次 Space 才发起下一轮并消耗 AI。测试误触风险已缓解；验证 Space 行为时注意在 3 秒窗口内截图。
- 结果页 theme chips 只统计「可注册」行（results-page.tsx 只遍历 availableRows）：验证 word/pinyin 配额补发时，若该路线候选全被注册则 chip 不显示，勿据此误判补发失败/未触发。
- 导航到 hunt 首页前先对该标签页执行 `sessionStorage.clear()`：`dh:lastSearch:v1` 会把上次结果页恢复出来，按首页坐标的盲点击可能命中「再来一轮」并真实消耗 AI 配额（实测踩坑）。
- R465 起恢复态（从 `dh:lastSearch:v1` 还原的结果页）「再来一轮」按钮也是两步确认：首点只变「再点一次确认」（3s 超时还原），3s 内二次点击才发起；本会话真实发起过一轮后护栏解除，单击直接发起。
- Before spending any real-AI budget, GET `/api/usage` and look at today's `aiErrors`: if `rate-limit`/`quota` is already climbing (or a prior hunt returned the 429 banner within ~3s with 0 candidates), the upstream API key is probably exhausted (`apikey_quota_exhausted` shows up as HTTP 429) and every further hunt will burn budget for nothing. Ask the lead to check `wrangler tail` instead of retrying; fall back to the synthetic `dh:lastSearch:v1` restore state for UI regression and mark timing as untested.
- Record the exact UTC click time of every real hunt (e.g. `new Date().toISOString()` in a click hook). `/api/usage` counts are global — parent-session curl probes or other users inflate `searches`, so reconcile "my N hunts" against the delta and label the rest explicitly.
- If `Emulation.clearDeviceMetricsOverride` alone leaves the tab stuck at 375, send from a fresh CDP session: `setDeviceMetricsOverride {width:0,height:0,deviceScaleFactor:0,mobile:false}` → `clearDeviceMetricsOverride` → `setTouchEmulationEnabled {enabled:false}` → `page.reload()`; verify with `innerWidth`.
- Row/Grid view choice is not persisted across reload (only density `dh:density:v1` is); re-click Grid after every `?cb=` reload before comparing brand cards.
- 构造合成 `dh:lastSearch:v1` 恢复态时，`values.style/lengthPref` 必须用空字符串（UI 默认）：worker 会把任何非空值拼成中文后缀「命名风格偏好：…」进 description，可能改变 `descriptionLooksEnglish()` 语言判定路径（实测踩坑：填 "auto" 导致英文描述照出拼音）。
- 错误态（quota / rate-limit）本地复现不必碰真实 key：Playwright `context.route("**/api/ai-search")` 用 `route.fulfill` 返回 NDJSON `{"type":"round",...}\n{"type":"error","round":1,"errorKind":"rate-limit"|"quota","detail":"llm-http-429"}`；R472 起 rate-limit 会起 30s 倒计时并自动重试一次（拦截路由会再收到一次请求），用 `page.clock.install()` + `clock.runFor(30000)` 推进而不是实等。现成脚本：`docs/qa/r472/errors.js`（74 项断言）与 `measure.js`（首屏几何）。
- 在可见 Chrome（录屏用）里复现错误态：Playwright `chromium.connectOverCDP("http://localhost:29229")` → `browser.contexts()[0].route("**/api/ai-search", …)` 对默认上下文生效（拦截会打到已打开的标签页），脚本保持进程存活（`process.stdin.resume()`）期间路由一直有效；用 `setsid nohup node … &` 启动、`pkill -f "^node live.js"` 结束（`pkill -f "node live.js"` 会连启动它的 bash 一起杀）。参考 `docs/qa/r472/live.js`。
- 恢复态两步确认「再点一次确认」只开 3s：在 computer 工具里必须把两次点击放进同一个 actions 批次（click → wait 0.7 → click），中间截图会超时还原。
- `/?mode=exact` 在 home-page.tsx 预选精确核验 tab，且 R472 起 App.tsx 对 `?mode=exact`（同 `?q`/`?tpl`）跳过 `dh:lastSearch:v1` 恢复（快照保留，回 `/` 仍能恢复）：从恢复态结果页点 quota 横幅「精确核验」必须落到首页精确核验 tab，回到结果页即回归。
- 375 下的结果页横幅（R472）：恢复条 + AI 理解条 + 微调 chips 折叠成一个 `button[aria-expanded]` 摘要行（`md:hidden`），chips 在展开面板里；桌面仍是原来的独立横幅（`hidden md:block`）。量 375 首屏时不要按桌面 DOM 找 `UnderstandingBar`。
- Typing Chinese via the computer-use `type` action (xdotool) silently drops characters into React textareas; put the text on the clipboard (`printf '…' | xclip -selection clipboard`) and press ctrl+v instead.
- R471 rule-based fallback: run wrangler dev with `.dev.vars` `LLM_API_BASE` pointed at a mock that returns 429 `apikey_quota_exhausted` (0 real AI calls). First search shows banner reason 「配额已满」/"quota exhausted" and trips a 5-min KV breaker (`dh:llm-breaker:v1`, delete with `npx wrangler kv key delete --binding CACHE --local "dh:llm-breaker:v1"`); later searches show 「配额已满，熔断中」/"quota exhausted, circuit open". The banner is `[role=status]` (amber) in App.tsx; 再来一轮/One more round buttons are `disabled` for quota reasons. Theme chips (`results.theme.*`) only render when >1 theme exists, so a pure-rule result set shows NO 「规则生成」/"Rule-based" chip — check meanings starting 「规则生成：」/"Rule-based:" instead. Banner `{count}` = number of rule labels, not label×TLD rows.
- Agent progress panel (round notes such as `agent.note.fallback`) is only mounted while `running`; the mock upstream finishes in ~1s. To observe it, throttle the tab via CDP `Network.emulateNetworkConditions` (downloadThroughput≈800 B/s, keep the CDP websocket open for the duration) so the SSE stream trickles for ~25s.
- To leave a CDP 375px emulation without reloading (in-memory state like the fallback banner is not persisted to `dh:lastSearch:v1`): send `Emulation.setDeviceMetricsOverride {width:0,height:0,deviceScaleFactor:0,mobile:false}` then `Emulation.clearDeviceMetricsOverride` and dispatch a `resize` event.
- Python sync Playwright route interceptor over CDP: `context.route()` handlers only run while the script's event loop is pumped — a `while True: time.sleep(1)` keeper silently never intercepts (the UI just spins with 0 results). Keep it alive with `page.wait_for_timeout(500)` in the loop (or use the Node variant). Verify with an intercept log line per request before trusting any error-state screenshot.
- Synthetic `dh:lastSearch:v1` rows need `scores:{length,readability,relevance,brandability}` — a wrong key (e.g. `brandable`) renders `NaN` badges. Rows with `status:"unknown"`/`"taken"` are hidden under the default 「可注册」 filter, so Grid card count = distinct *available* labels.
- After a rule-based fallback run, the worker's final summary `proposed` event has `items:[]`. Since R476 App.tsx only calls `clearAiQuotaDown()` when a `proposed` has non-empty items with no `theme:"rule"`, so `dh:aiQuotaDown:v1` survives a fallback run and the home 「AI 猎名暂不可用」 banner persists across reload (on 201573e5 and earlier it was wrongly cleared). Also since R476 the fallback banner at <640px is a 58px one-line `<details>` summary (click `[role=status] summary` to expand full text); the full text ends with 「预计约 N 分钟后可重试 AI」/"retryable in about N min" for quota reasons, driven by `fallback.retryAfterS` (worker) → `SavedFallback.retryAt` (sessionStorage snapshot).
- Raw-CDP alternative when Playwright is not installed: python `websockets` (`websockets.connect(url, origin=None)`) against `http://localhost:29229/json` works for Emulation/Page.captureScreenshot.
- The home 「AI 猎名暂不可用」 banner (`home.aiDownBanner`) only renders while the AI naming tab is active (`home-page.tsx` `searchMode === "ai"`), so `/?mode=exact` never shows it — to verify persistence, reload then click the AI tab.
- 375px first-`[data-brand-card]`-top targets differ between the live results page and the restored one: a `?cb=` reload adds the R472 「已恢复 · …」 summary row (~+30px). Measure the live state for the tighter target (R476 zh live ≈378, restored ≈407; both must stay ≤420).
- On the zh home, pasting a long description reflows the 「开始猎取」 button onto a new line — re-screenshot to locate it before clicking.
- Home example-prompt chips (`EXAMPLES`/`EXAMPLES_EN`, the 「试试一个示例，一键体验」 row) call `submit()` on click and DO fire `/api/ai-search` — verify their labels visually only. The industry template labels (`TEMPLATE_LABELS`: 茶叶品牌/跨境电商/独立开发者…) only `setDescription()` and are safe to click; the 「精确核验」 tab posts to `/api/search` (non-AI), not `/api/check`.
- R478 SSR hero skeleton: `homeHeroSkeleton()` (content/ssr-html.ts) injects badge/h1/subtitle into `#root` before React mounts. To prove no flash, drive Chrome via raw CDP (`Page.navigate` then loop `Page.captureScreenshot` every ~120ms;) and compare frame 0 (skeleton, `react:false`) with frame 1+ (hydrated) — only expected diff is a ~7px shift when the scrollbar appears. The `getBoundingClientRect` probe at frame 0 can run before the stylesheet applies (h1 left≈8, width≈full) even though the painted screenshot is styled — judge by pixels, not by the early geometry probe. Text should be identical to `HOME_HERO[lang]` in content/home-copy.ts.
- The visible Chrome is 1600×1122 (screenshots are downscaled to 1024×768), so "1280px" checks need `Emulation.setDeviceMetricsOverride {width:1280}`; the toggle header buttons in the downscaled screenshot sit at ≈(856,73) EN/中 and ≈(889,73) theme.
- R481 pageview counting (`pv:{date}:<shard>` per-isolate KV keys since R482, summed by `/api/usage`; the aggregated `/api/usage` value is now exact across isolates, the old "lower bound" caveat is gone). Freshly written shards can take up to ~60s to become visible to `/api/usage` (KV eventual consistency): a read 9s after a burst showed +0, a read 60s later showed the exact +13 — wait ≥60s before declaring a loss only sees requests that reach the Worker: HTML pages are served with `Cache-Control: public, max-age=600`, so a plain address-bar navigation to a recently visited page may be answered from Chrome's HTTP cache and NOT increment `pageviews`. Use Ctrl+Shift+R (or a fresh `?cb=` query) when asserting count deltas, and hard-reload `/api/usage` too (it is `max-age=300`). Any `curl -A Mozilla` header probe against an HTML page also counts as a human pageview — reconcile deltas against your own probes.
- The footer (with the R481 `footer.analyticsNotice` privacy sentence) is only rendered in `home`/`results` mode (App.tsx `mode === "home" || mode === "results"`); /tld, /vs, /prices, /why, /advanced etc. have NO `<footer>`. `document.querySelector('footer p')` matches the TLD-guides heading first — filter by `textContent.includes('Cloudflare Web Analytics')` before measuring the notice.
- Head injection (`meta[name=google-site-verification]`, `meta[name="msvalidate.01"]`, `script[data-cf-beacon]`) can be toggled locally via `npx wrangler dev --var GSC_VERIFICATION:… --var BING_VERIFICATION:… --var ANALYTICS_PROVIDER:cloudflare --var ANALYTICS_TOKEN:<32 hex>`; run a second no-vars instance on another port for A/B. The beacon host static.cloudflareinsights.com may be blocked locally — assert the tag in the DOM, not the network load.
- Baidu (R485): `--var BAIDU_VERIFICATION:codeva-…` adds `meta[name=baidu-site-verification]` via the same injection path. The Baidu push cron only runs with BOTH `--var BAIDU_PUSH_SITE:https://hunt.zalize.com --var BAIDU_PUSH_TOKEN:<fake>`; NEVER point a fake token at the real endpoint — add `--var BAIDU_PUSH_ENDPOINT:http://127.0.0.1:9999/urls` and run a local mock (Node `http.createServer` that logs method/URL/content-type/body and returns `{"remain":N,"success":M}` / `{"error":400,"message":"over quota"}`). Start wrangler with `--test-scheduled` and trigger `curl 'localhost:8787/__scheduled?cron=0+*/6+*+*+*'`; the cron also fires `pingIndexNow` (real Bing request), so first seed `npx wrangler kv key put --binding CACHE --local indexnow:last "$(date +%s000)"`. Inspect state with `npx wrangler kv key list/get --binding CACHE --local` (`baidu:last`, `baidu:lastAttempt`, `baidu:lastError`, `baidu:pushed`) and `/api/usage` (`baiduLast`, `baiduLastError`); delete `baidu:last`/`baidu:lastAttempt` between runs to bypass the 23h/5h gates (R514; were 24h/6h). Without the vars, no `baidu:*` key must ever appear.
- `curl /no-such-page` without `Accept: text/html` returns a 404 with an empty body (pre-existing, same on base); add `-H "Accept: text/html"` to get the branded shell for grep checks.
- Registrar affiliate (R480+): `GET /api/registrars` is served with `cache-control: public, max-age=300` and the frontend fetches it after load, so after switching the dev server between DEFAULT (`npx wrangler dev --port 8787`) and CONFIGURED (`--var 'REGISTRAR_AFFILIATE_JSON:{...}'`) the tab keeps showing the *old* config even after Ctrl+Shift+R. Refresh the cache entry from the console with `fetch('/api/registrars',{cache:'reload'})` then F5 before asserting hrefs/rel/disclosure. `curl /api/registrars` is the ground truth for what the worker serves.
- The session Chrome (CDP :29229) has instrumentation that strips `target="_blank"` from anchors (and adds `devinid` attrs), so register links navigate in the same tab and DOM checks for `target` are inconclusive there. Verify `target`, popup URL, keyboard focus (`document.activeElement`) and beacon bodies in a clean headless Chromium via `playwright-core` (`/opt/.devin/chrome/chrome/linux-*/chrome-linux64/chrome`, `chromium.launch({executablePath, headless:true})`, `page.on('request')` → `postData()`); `ctx.route(/porkbun|namecheap|dynadot|aliyun|tencent|cloudflare/…fulfill)` blocks real registrar visits. Registrar set since R503: porkbun/namecheap/dynadot/cloudflare/aliyun/tencent; `.cn`-family menus show aliyun → tencent → dynadot (Porkbun/Namecheap/Cloudflare hidden), dynadot.com search pages sit behind a Cloudflare challenge so never navigate to them.
- `POST /api/click` is a `navigator.sendBeacon` (DevTools type `ping`): the Network panel shows status 204 and Content-Length but no Payload tab; use the headless request capture above for the exact body. Turn on "Preserve log" before clicking, since clicking a register item in the session Chrome navigates away.
- Outbound/usage concurrency proof (R487, zero-AI): `node scripts/verify-r487-local.mjs <base> [<base2>]` fires N (default 12) concurrent `POST /api/click {registrar:"aliyun",tld:"cn"}` and asserts `outbound.aliyun`/`outboundByTld.cn` in `/api/usage` grew by exactly N (it waits `SETTLE_MS`, default 2.5s locally / 65s for non-localhost bases). Locally, run two `wrangler dev` instances on different `--port`/`--inspector-port` sharing one `--persist-to` dir to get two real isolates (= two shard keys, visible with `npx wrangler kv key list --binding CACHE --local --persist-to <dir>`). On base 8351a69 the same 12-click run landed only +2; on R487 it lands +12 from either instance. Against production, `N=12 node scripts/verify-r487-local.mjs https://hunt.zalize.com` writes 12 real `aliyun`/`cn` outbound clicks into today's usage — note them in the audit log.
- `pkill -f "wrangler dev"` often exits -1 without killing anything; list pids with `pgrep -af "wrangler|workerd"` and `kill` them explicitly, then confirm `ss -ltnp | grep 8787` is empty before restarting.
- Home quick-register chips only render for RDAP-*available* exact domains: `zalize.cn/.com`, `tailwag.com`, `petwan.com` are taken; `zalizepetwan.com/.cn` and `petwan.cn` were available at test time. Hovering the chip shows title 「去 <registrar> 注册 <domain>」.
- Results row thumbs-down (「不喜欢」) sits directly left of the register menu items when the menu is open near x≈548 — a stray click there marks the whole label disliked (all `petwan.*` rows); click it again to undo.
- Shortlist page 「清空 → 确认清空？」 did not clear the list on two confirm clicks (pre-existing, unrelated to R480); per-row 「移除」 works.
- All destructive two-step buttons (/monitors 「取消监控 → 确认取消？3」, shortlist 「我的分享链接 → 删除 → 确认删除？」, 「清空」) share the same ~3s confirm window: put both clicks in ONE computer `actions` batch (click → wait 1 → click); a screenshot between them lets the timer expire and the button silently reverts, which looks like a bug but isn't. The 「清空」 failure above may be the same timing issue.
- Zero-AI way to exercise every `RegisterMenu`/`RegistrarAnchor` surface: shortlist 「去注册」 (row menu) → 「生成分享链接」 (POST /api/share, no AI) → open `/s/<id>` and use its 「去注册」 menus → toggle a row's 「监控」 switch (takes one global monitor slot, `/monitors` then shows 「去注册 · <primary registrar>」 only for RDAP-available domains) → cleanup: 取消监控 (slot count returns), delete the share (GET `/api/share/<id>` → 410 confirms revocation), remove the row. A `dh:myShares:v1` key (`[]`) is left in localStorage afterwards — restore from the pre dump.
- The shortlist 「我的分享链接」 block only appears after a share exists and pushes the table down ~85px: re-screenshot before clicking row toggles, or the click lands on the wrong row/nothing.
- Breadcrumb `nav[aria-label="面包屑导航"]` exists only on `/tld/*`, `/guide/*`, `/vs/*`; `/why` and `/prices` have none by design — don't report that as a regression.
- "AI is down, expect fallback" is a *moving* precondition: on 2026-09-04 21:01Z the authorized 「茶叶电商，寓意清雅」 hunt hit a recovered DeepSeek (`understanding` event, 3 rounds, `llmProvider.primary` +3, zero `fallback` events), so the R489 rule path was NOT reached in prod. Before spending the one authorized search, decide with the lead what counts as evidence if the upstream is healthy. The rule-fallback output for any input can always be previewed with zero AI via `node scripts/bench-zh-rule-fallback.mjs --labels` (edit `INPUTS` in the script or add a case) — use it to pin the expected label set / kinds / bad-example count *before* the real hunt, and compare afterwards.
- Capture the AI hunt's NDJSON with a Playwright `page.on('response')` hook attached to the session tab *before* clicking 「开始猎取」 (`response.body()` resolves when the stream ends; save to a file). Event types: `round`, `understanding`, `proposed{items[{label,meaning,theme}]}`, per-domain rows `{domain,status,method,expiresAt}`, `fallback{reason,count,retryAfterS}`, `done{availableCount,reachedTarget}`. Pasting Chinese into the textarea via `xclip -selection clipboard` + Ctrl+V reflows 「开始猎取」 to a new line — re-screenshot before clicking.
- Independent RDAP spot-check: `curl -s -o /dev/null -w %{http_code} https://rdap.org/domain/<d>` → 404 = unregistered, 200 = registered (works for .com; .cn has no public RDAP, use `POST /api/check {"domains":[...]}` which returns NDJSON `{domain,status,method:"whois",expiresAt,cached}` — note `cached:true` means it's the same cache the page used, not an independent source). rdap.org can take >10s; run it in the background.
- Hub filters (`hub-filter.tsx`) are lowercase substring matches over label/summary; since R510 a dotless query also matches the dot-stripped text at a word boundary, so on `/vs` `com vs cn`, `.com vs .cn` and `com-vs-cn` all → 1/444 (`io vs ai` → 1/444 = the real `/vs/io-vs-ai`, not `.studio vs .ai`). Dotted queries stay literal: `/tld` `.com` → 7/408 while `com` → 166/408; `cn` alone on `/tld` → 2/408 (.cn + .br summary).
- The console hook logs the browser's network message `Failed to load resource: … 404` as `[error]` when you visit the branded 404 page on purpose — it is not a JS/page error; filter it out when asserting "0 console errors". 404 shells are also not counted in `pageviews.other` (worker.ts pageview comment), so don't expect a pv delta from them.
- `xdotool type` of CJK text into the hub search input silently produced nothing in this session Chrome; paste via clipboard instead (`printf '实名' | xclip -selection clipboard`, then triple-click + Ctrl+V).
- Footer "counts" on the home page are not printed as numbers: assert `footer a[href^="/tld/"]` / `/guide/` / `/vs/` link counts (408/410/444) plus the three 「浏览全部 →」 links. Guide/tld/vs content pages have no footer at all.
- R491/R492 SSR-skeleton audits: the browser's `navigator.languages` is `en-US`, so a bare `/` or `/why` renders the EN SSR shell (R492 `resolveLang`). For deterministic zh skeleton frames, send `Network.setExtraHTTPHeaders {"Accept-Language":"zh-CN,zh;q=0.9"}` over raw CDP or add `?lang=zh`; production assets are fast/cached, so to actually *see* the pre-React frame add `Network.setCacheDisabled` + `Network.emulateNetworkConditions` (~150 KB/s) and use a fresh `?cb=` (see `/home/ubuntu/r491/frames_zh.py`). Probe per frame: `react` (root has React fiber), `h1` count/text/top, `footer` count, `#root` child count.
- main.tsx `routeModule()` only waits for the route chunk on `/`, `/tld/*`, `/guide/*`, `/vs/*` — any other SSR-skeleton page (`/why`, `/advanced`, …) mounts immediately and the lazy route's `<Suspense fallback={<PageFallback/>}>` clears the skeleton until the chunk arrives. Under throttle `/advanced` shows H1 → blank main (~0.6 s) → H1; `/why` (small chunk) didn't. Check this gap whenever a new SSR skeleton is added; on an unthrottled cached load frame 0 is already hydrated so the gap is invisible.
- The hydrated home footer is ~1270 links tall; coordinate clicks on it from the downscaled screenshot are unreliable (landed on `/tld/lk` instead of `/why`). Click footer links with a Playwright locator on the CDP tab (`page.locator('footer a[href="/mcp?lang=zh"]').click()`) — it's a real mouse click that shows in the recording. Wait for `load` first or you get "Execution context was destroyed".
- Header EN/中 toggle only writes `domainhunter:lang`; it does not rewrite the URL's `?lang=` param, and `loadLang()` lets `?lang=` win and re-persist on reload. So toggle → F5 on a `?lang=xx` URL reverts the language (pre-existing since 2026-08-06, more exposed now that R491 footer/site links all carry `?lang=`). Don't report the URL/UI mismatch itself as new; do mention the F5 revert if it matters.
- Home 「精确核验」 with a direct name (e.g. `zalizetest123`) auto-fires exactly one `POST /api/search` and shows the 「看起来是现成的名字?」 chip grid; clicking 「立即核验」 afterwards adds no extra request and does not touch `domainhunter:recent-searches` or `searches`/`fast` in `/api/usage`. `/advanced` bulk paste also uses one `POST /api/search`. Watch `/api/` requests with `page.on('request')` on the CDP tab (`/home/ubuntu/r491/netwatch.py <seconds>`).
- R495 (a7ecf5a): `routeModule()` now also awaits the `/why`, `/advanced`, `/mcp` chunks, and the header 中/EN toggle rewrites an existing `?lang=` via `history.replaceState` (bare URLs stay untouched). Re-run `frames_zh.py` on any *new* SSR-skeleton route that is not in `routeModule()`; assert `h1n===1` in every frame (the pre-fix signature was a `react:true, h1n:0` frame ~1.5 s in under 150 KB/s throttle). For the toggle, assert `location.href` immediately after the click and again after F5.
- Registrar-menu surfaces (R503+): the home quick-check Available chip is a SINGLE `RegistrarAnchor` to `primaryRegistrar()` (阿里云 for .cn/.com.cn, Porkbun otherwise) — there is no dropdown on the home page. To exercise the full `RegisterMenu` with zero AI, star the quick-check chips (the ☆ button right of each Available chip) for a random name such as `qwzx7k3` (.com/.cn appear at once; .com.cn only after 「查更多后缀 +N」), then open 「去注册」 on `/shortlist` rows. Expected menus: `.cn`/`.com.cn` → 阿里云, 腾讯云, Dynadot (hint 「中文 · 支付宝」/"CNY · Alipay"); `.com` → Porkbun, Namecheap, Dynadot, Cloudflare, 阿里云, 腾讯云. Registrar names are never localized. Removing rows via the per-row 「移除」 puts `domainhunter:shortlist` back byte-identical (addedAt preserved) — still diff against the pre-dump.
- `/shortlist` renders BOTH the desktop `<table>` and the mobile card list in the DOM (CSS-hidden); Playwright `button:text-is('去注册')` matches 12 buttons for 6 rows and `scroll_into_view_if_needed` on a hidden one times out — append `>> visible=true` (visible count = row count, same order as the list). Keyboard path on a row: Tab from the 「复制」 button ×2 → 「去注册」, Enter opens the Radix menu, ArrowDown×N focuses the Nth item (`document.activeElement` is the `<a>`).
- Headless popup/beacon capture for registrar items works with the *Python* playwright package already installed (`p.chromium.launch(executable_path="/opt/.devin/chrome/chrome/linux-137.0.7118.2/chrome-linux64/chrome", headless=True, args=["--no-sandbox"])`, seed `domainhunter:shortlist` on a `/404-seed` page first, `ctx.route(...)` stub for registrar hosts, `ctx.expect_page()` around the click). Each captured click writes a real `outbound.<registrar>` / `outboundByTld.<tld>` into today's `/api/usage` — record the UTC time and reconcile it in the zero-AI diff (searches/fast/refine/aiErrors must still be +0).
- `qwzx7k3.com.cn` on `/shortlist` shows 「—」 in 参考价 (no static reference price for `com.cn`, while `.cn` shows 首年 ¥29) — pre-existing, not a registrar-menu regression.
- R504: `/api/usage` top-level has `indexnowPending` (number of sitemap URLs not yet accepted by IndexNow; 1270 = full sitemap right after the 429 self-lock). The 6h cron drains ≤3×100 per run, so expect it to fall by ≤300 per cron; `indexnowLastError` keeps the old 429 record until the next successful batch.
- R514: `/api/usage` also exposes `indexnowLastAttempt`. Read the trio together: `indexnowLastAttempt` not advancing with `cronLast` = the cooldown gate skipped the run (this is exactly what happened 2026-09-05 18:00Z: attempt 12:00:17.244Z vs cron 18:00:17.150Z, 94 ms short of the old 6h gate); advancing together with `lastError.at` = attempted but upstream failed. Gates are now 5h retry / 23h success, deliberately shorter than the 6h cron. To test IndexNow locally without hitting Bing: `--var INDEXNOW_ENDPOINT:http://127.0.0.1:9998/indexnow` + a Node mock that returns 200, then seed `indexnow:lastAttempt` to `now-6h+3s` (must push 3×100, pending 1270→970), `now-5h+3s` (must skip), `now-5h-3s` (must push again). `rm -rf apps/web/.wrangler/state/v3/kv` afterwards so the local pushed snapshot does not leak into later runs.
- R515: `/api/usage.indexnowLastResult` `{at,ok,status,message,submitted,retries}` is written on every run that actually sent a request (success or failure); `indexnowLastError` also carries `retries`. Read it with the trio above: `ok && retries=0` = direct success; `ok && retries>0` = a batch got 429 and succeeded after the in-run 60 s backoff (max 2 retries per batch, `INDEXNOW_429_*` in `indexnow.ts`); `!ok && status=429 && retries=2` = still throttled after all retries — then change strategy (offset from the top of the hour / longer backoff), do not just wait. `indexnowPending` dropping by ≤300 per cron is the only proof that a snapshot advanced; `indexnowLast` stays at the old date until the snapshot covers the whole sitemap (expected, not a bug). To exercise the retry locally, make the mock return 429 on the first request and 200 afterwards; the `__scheduled` curl returns "Ran scheduled event" immediately while the worker keeps running in the background — poll `/api/usage` ~65 s later, expect 4 mock hits (429, 200, 200, 200 with the first two bodies identical), pending 1270→970 and `indexnowLastResult.retries=1`. Real production `sleep` inside the cron is fine: worst case 3 batches × 2 × 60 s = 6 min < the 15 min cron wall-time limit.
- R517: production 06:00Z run still `!ok, status=429, retries=2` — an independent probe Worker showed the cause is source-IP throttling of the shared Workers egress by `api.indexnow.org`/`www.bing.com` (5 URLs → 429 from Workers, 200 from the dev box the same minute), while `yandex.com`/`search.seznam.cz`/`searchadvisor.naver.com`/`indexnow.yep.com` accept (202/200). So a local `curl` 200 to IndexNow proves NOTHING about production. The worker now re-sends the same batch to `INDEXNOW_FALLBACK_ENDPOINTS` (in that order) as soon as the main endpoint answers 429; only when every endpoint is 429 does the 60 s backoff start. Read `indexnowLastResult.fallbackHosts` (string[], present only when some successful batch landed on a fallback): `ok && fallbackHosts=["yandex.com"] && retries=0` = main endpoint throttled, batch accepted by Yandex without waiting (the protocol shares submissions across all engines); `ok` without `fallbackHosts` = main endpoint accepted directly; `!ok, status=429, retries=2` = ALL endpoints throttled 3 rounds — then reconsider (Bing-only key issue? all engines throttling the egress?). `indexnowPending` dropping is still the only snapshot proof. Locally: `--var INDEXNOW_ENDPOINT:http://127.0.0.1:9998/indexnow --var INDEXNOW_FALLBACK_ENDPOINTS:http://127.0.0.1:9998/yandex` with a mock that returns 429 on `/indexnow` and 202 elsewhere → 6 hits alternating 429/202, no sleep, pending 1270→970, `fallbackHosts=["127.0.0.1:9998"]`. Setting `INDEXNOW_ENDPOINT` alone disables fallbacks (never points a mock run at real engines); `INDEXNOW_FALLBACK_ENDPOINTS=""` disables them explicitly in production.
- R509 share statuses: `POST /api/share` now carries `status` per item and `/s/:id` renders 可注册/已注册/未知 badges; a taken row is struck through and has NO 去注册 menu; the copy button counts only `available` rows (「复制 N 个可注册」). A snapshot with NO `status` fields (legacy, or a hand-made `curl -X POST /api/share -d '{"items":[{"domain":"x.com"}]}'`) shows the amber 「此快照未记录核验状态…」 line and 「复制 N 个域名」, and its SSR `<title>` is the neutral 「N 个候选域名 | DomainHunter」. Mixed available+taken snapshots also get the neutral title; only all-available gets 「N 个可注册域名候选」. Creating a no-status share via curl is the easiest way to exercise the legacy branch when `dh:myShares:v1` has no old id.
- 「我的分享链接 → 删除」 is a 2-step confirm (「确认删除？」) that reverts after a few seconds; issue both clicks in ONE computer-use batch (~0.5s apart) or the second click just re-arms the confirm. A revoked `/s/:id` returns HTTP 410 + noindex + 「分享已撤销 | DomainHunter」 (R510).
- Reference-price surfaces: home quick-check chips show static prices in USD (`≈$${toUsd(first)}`, e.g. ¥33 → `≈$5`), never `¥`; assert CNY values on `/prices` (row text `≈$5 ¥33` / `≈$5 ¥38` for .cn since R509) and `/tld/cn` (「静态参考价：首年 ¥33 · 续费 ¥38/年」). There is no `/tld/com-cn` or `/tld/com.cn` page (404) even though `com.cn` has a static price in `types.ts`.
- Hub filters match different candidates: `/vs` matches slug (`com-vs-cn`), `.a vs .b` and titles, dotted or not (R510); `/tld` filter `cn` also matches `.br` via its description text (2/408). `/guide` filter `saas` → 2/410, `b2b` → 2/410.
- R510 share shells: `/s/<id>` now returns the SPA shell with the *real* status — 200 live, 410 revoked, 404 unknown/invalid id (regex `^[\w-]{1,32}$`; invalid ids get canonical `/s/`) — each gone state adds `<meta name="robots" content="noindex" />` and a localized `<title>` (zh 「分享已撤销」/「分享不存在或已过期」, en "This share has been revoked"/"Share not found or expired"; picked by `Accept-Language`/`?lang=`, so the *tab title* may be EN while the SPA body is zh from localStorage). Because the document itself is 410/404, the console shows TWO `Failed to load resource … 410/404` lines per visit (shell + `/api/share/<id>`) — both are network noise, not JS errors. Quickest zero-AI recipe: `curl -A Mozilla -X POST /api/share -d '{"items":[{"domain":"x.com","status":"available"}]}'` → assert 200 + 「N 个可注册域名候选」 → `DELETE /api/share/<id> -d '{"token":…}'` → assert 410 shell; never leave the share live.
- /prices 375px since R510: the 「续费↑」 badge (`.bg-amber2-dim`) is `whitespace-nowrap` — assert `getClientRects().length===1` and width > height (≈37×24px) on all visible badges; the pre-fix stacked badge was ~18px wide and >40px tall. The badge still *wraps beneath* the amount at 375px (`flex-wrap`, intentional) and sits on the same row only from ~640px up; `scrollWidth` must stay 375. Filter `cn` there to check the .cn static row (`≈$5 ≈$5`, ¥ hidden below `sm`).
- `Emulation.clearDeviceMetricsOverride` from a *new* CDP session sometimes leaves `innerWidth` at the emulated value (seen after a 1280×900 override); send `Emulation.setDeviceMetricsOverride {width:0,height:0,deviceScaleFactor:0,mobile:false}` immediately followed by `clearDeviceMetricsOverride`, then F5 — `innerWidth` returns to the real window (1600). Also, computer-use clicks aimed at header buttons will miss while an override is active (the page is drawn in the top-left 1280px only).
- `/tmp` helper scripts (dump/restore storage, m375) do not survive between sessions — recreate them (~30 lines each, Playwright `connect_over_cdp("http://localhost:29229")`, pick the tab whose url contains `hunt.zalize.com`, dump BOTH localStorage and sessionStorage) before touching production. Deleting a share with curl needs no UA; HTML/SSR GETs do (`-A Mozilla`).

## R519–R522 gotchas (content pages: FAQ / chips / /vs price table)
- SSR-vs-hydrated comparisons: `/tld/*` chips carry prices, and the client re-renders them from `/api/prices` after mount (static `¥79` → live `$12.52 ≈¥90`). Compare **slugs/hrefs** for structure and treat label drift as a separate finding; `/guide` and `/vs` chips are price-free and should be byte-identical.
- FAQ answers: JSON-LD `acceptedAnswer.text` must equal the `<details>` text with the anchor `<a>` stripped; anchor targets use `scroll-mt-20`, so a landed heading sits at ~80px from the viewport top — assert `getBoundingClientRect().top` in 60–120.
- Hub landing (`/tld|/guide|/vs?lang=..#hub-g-<g>`) scrolls after mount; wait ≥1 s before reading the section top. `/tld/at`-style TLDs live in the fallback group `more`.
- `/vs` price table: `th scope` is `col×4 + row×3` — don't count the row headers as columns. `/prices` SSR is a skeleton; read numbers from the hydrated DOM (filter box) or `/api/prices`.
- 375px: CDP `Emulation.setDeviceMetricsOverride` on the recorded tab; the table wrapper (`overflow-x:auto`) should scroll internally while `documentElement.scrollWidth` stays 375.
- Lighthouse: `/opt/hostedtoolcache/node/22.23.2/x64/bin/lighthouse <url> --preset=desktop --only-categories=seo,accessibility --chrome-flags="--headless=new --no-sandbox" --output=json` works without extra installs.
- Visiting `?lang=en` pages rewrites `domainhunter:lang` — expect that key to need restoring even if you never clicked the language toggle.

## R524–R528 gotchas (SSR price snapshot, price-free chips, share-URL cleanup)
- Since R528 `/tld/*` and `/guide/*` SSR and the client share one KV price snapshot (`content.prices` injected → `pickPrices`), so SSR price-card text and 「相关 TLD」 chip labels should be **byte-identical** to the hydrated DOM again; any drift is a real regression now. Always put an explicit `?lang=zh|en` on both the curl and the browser URL — otherwise the browser follows `domainhunter:lang` (rewritten by earlier `?lang=en` visits) and you compare zh SSR with en DOM.
- 「更多 TLD 指南 / More TLD guides」 chips are price-free (`^\.[a-z0-9-]+$`); only the 6 「相关 TLD」 chips keep `priceShort`. Link-share recompute: `thin-analyze.mjs` hard-codes `GROUPS = ["tld","guide","vs"]` and crashes if `html/guide` is missing — after a `--groups tld,vs` fetch, `mkdir -p <out>/thin/html/guide` and ignore the NaN guide rows. `linkShare` is not in `summary.json`; compute `1 − proseWords/bodyWords` from `pages.csv` (R528: tld/en median 15.3 %, >25 % = 0; tld/zh 12.3 %/0).
- FAQ visible-vs-JSON-LD check: strip `<summary[^>]*>…</summary>` (the summary carries classes); production hreflang values are `zh` / `en` / `x-default` (not `zh-CN`).
- Shortlist share lifecycle (R528): `visibleShareUrl()` hides the 「分享链接（30 天有效）」 row when the record is deleted from `dh:myShares:v1` OR when the shortlist has 0 items; the 「我的分享链接」 history card stays while the record is live, so revoke there. The 「删除→确认删除？」 window is `CONFIRM_WINDOW_MS = 5000` — a screenshot/zoom between the two clicks already exceeds it and the second click merely re-arms; batch `click, wait 1, click`. The DOM button's `textContent` is 「删除确认删除？」 (both labels present, one hidden) — use `innerText` to read the visible state.
- Unknown paths return an EMPTY 404 body unless the request sends `Accept: text/html` (worker.ts `notFoundShell` gate); browsers always do, bare `curl -A Mozilla` doesn't — add `-H 'Accept: text/html'` before grepping the branded shell.
- `document.documentElement.className` is `""` in dark mode (dark is the default; only `light` is added), so assert theme via `localStorage['domainhunter:theme']` or the class `light`; toggle theme with the header button, not by editing classes (that leaves storage stale).

## R531 gotchas (static reference prices, /vs pick cards)
- Check `apps/web/src/main.tsx` before interpreting SSR-to-client transition failures: the current app uses `createRoot().render`, not `hydrateRoot`. Delaying route JavaScript and capturing compositor frames can expose a brief blank content area during replacement even when all nonempty price text is identical. Report price-content drift separately from mount blanking; verify any suspected regression on a baseline worktree with the same timing probe. Absence of hydration warnings alone does not prove stable first paint.
- When running a baseline Wrangler beside the main app, use both a different HTTP port and inspector port (e.g. `npx wrangler dev --port 8788 --inspector-port 9230`). Root and apps/web node_modules symlinks suffice for an isolated `npx vite build` when packages/core has no node_modules; do not assume every workspace has its own dependency directory.
- `/vs/*` pick cards (`<p class="tnum mt-1 text-xs text-txt2">` under each `.tld` heading) now read the same injected KV snapshot as the `/vs` price table (`priceFull(tld, lang, snapshot.live)` on both sides), so SSR text must be byte-identical to the hydrated DOM and equal to `/api/prices` (`Porkbun 实时价：首年 $X（≈¥…）· 续费 $Y/年（汇率 7.2 估算）` / `Porkbun live: $X 1st yr (≈¥…) · renews $Y/yr (¥ est. at 7.2)`). Only TLDs missing from the snapshot (cn, com.cn, so, uy, ar, … — 58 static-only TLDs) may show 「静态参考价 / Static reference」. Compare with explicit `?lang=` on both sides (see R528 above).
- Static reference prices (`TLD_PRICES` in apps/web/src/types.ts, CNY) are audited with `node scripts/check-static-prices.mjs <api-prices.json>` or `--live` (GET production `/api/prices`, zero AI). It prints the static-vs-live table (`staticUsd = round(first/7.2)` vs Porkbun registration, same method as docs/audits/r527/static-vs-live-prices.json), lists the >50 % / >30 % sets and the 58 static-only TLDs, and exits 1 when any first-year deviation exceeds 50 % (`--threshold=0.3`, `--renew` to gate renewals too, `--json=<file>` to save rows). Run it whenever `/api/prices` upstream changes or before touching `TLD_PRICES`; refresh with `¥ = round($ × 7.2)` keeping `first`/`renew` separate (promo TLDs like xyz/fun/me/app have a low first year and a high renewal). Baseline evidence: docs/audits/r531/.

## R532–R534 gotchas (bilingual 404, CSP Report-Only, /api/csp-report, Lighthouse on 404)
- 404 shell (R532): unknown page-like paths (no `/api/` prefix, no file extension) return the shell for EVERY `Accept` (the old `Accept: text/html` gate is gone); `/api/nope` and `/nope.png` still return 0 bytes. Title is bilingual by `?lang=`/`Accept-Language` (「页面不存在 | DomainHunter」 / “Page not found | DomainHunter”), no canonical, `noindex`; the SPA sets the same title via `usePageTitle`, so assert `document.title == SSR <title>` per language and that the header EN/中 toggle re-titles the tab.
- CSP Report-Only (R533): `content-security-policy-report-only` with a per-request nonce on HTML only (JSON/API responses get just nosniff + referrer-policy — assert the header is ABSENT on `/mcp` JSON, `/api/*`). JSON-LD `<script type="application/ld+json">` intentionally has no nonce. Look for violations via DevTools Console `[Report Only]` lines AND a Playwright `request` hook on `/api/csp-report` — a clean run has 0 browser POSTs. Chrome's Issues panel shows a pre-existing “form field should have an id or name” a11y hint on the home textarea; it is not CSP.
- `/api/csp-report`: every accepted valid report (204) increments `days[d].cspReports` and adds a `cspSamples` entry (`directive`, `blockedUri` without query string, `count`, `firstAt`, `lastAt` — no document-uri/UA/IP). This is a non-reversible production write: plan the exact number of valid bodies you will send (413 bodies and garbage JSON do NOT count) and reconcile `cspReports`/`cspSamples` in the usage-after diff. >16384 B by Content-Length OR by streamed body (chunked) → 413.
- Lighthouse navigation mode refuses 404 documents (`ERRORED_DOCUMENT_REQUEST (Status code: 404)`, all categories null). To score the branded 404 page use flow **snapshot** mode: puppeteer-core + chrome-launcher (both ship inside the global lighthouse install) → `page.goto(404 url)` → `startFlow(page).snapshot()`; see `/home/ubuntu/r534/lh_snapshot_404.mjs` for a 30-line recipe (scores a11y/BP/SEO 100 on R533).
- 44 px touch targets: inline links use `.tap-target` whose `::before` pseudo-element extends the hit zone to 44 px WITHOUT changing the element's box (`getBoundingClientRect().height` stays 20–28 px). Assert with `getComputedStyle(el,'::before').height === '44px'` and `document.elementFromPoint(x, centerY ± 21) === el`, not with the element height. Only block links (404 page buttons/chips, `min-h-[44px]`) have a ≥44 px box.
- Quick-check shortlist rows carry `status:"available"` into `/api/share`, so a share mixing them with an old status-less row (petlovo.ai) gets the neutral title 「N 个候选域名」, not 「N 个可注册域名候选」 — expect that before calling it a bug.

## R535–R540 gotchas (static-asset headers, lazy price chunk, textarea a11y, /vs verdict parity, MCP schema)
- Static-asset headers come from `apps/web/public/_headers` (`/*` → nosniff + referrer-policy only). Assert with `curl -sI -A Mozilla` on `/favicon.svg`, `/site.webmanifest`, `/assets/<bundle>.js`, `/fonts/*.woff2`, `/robots.txt`; they must NOT carry HSTS/CSP-RO/XFO (those are HTML-only from the worker middleware). JSON (`/api/prices`, `POST /mcp`) has nosniff + referrer-policy and no `content-security-policy*`.
- `TLD_PRICES` static values are NOT in the main `index-*.js`: they live in a lazy chunk `assets/prices-*.js`. Find it via `grep -o 'assets/prices-[A-Za-z0-9_-]*\.js' index-*.js` (or the modulepreload list) before grepping `tld:{first:N,renew:M}`. Same short keys appear for other TLDs (`cash:{first:70…}`, `eco:{first:430…}`) — anchor the grep with the TLD name, not the value.
- Home textarea now has `id="home-description" name="description"` and a mode-dependent `aria-label` (zh 描述你的品牌或项目 / 输入要核验的名字或域名; en Describe your brand or project / Name or domain to check); `/advanced` has `id="advanced-bulk" name="bulk"` (粘贴要批量核验的名字或域名 / Paste names or domains to bulk check). Probe with `?cb=` reload — a tab still holding the pre-deploy SPA shows the old attributes.
- 精确核验 typing fires an automatic non-AI `POST /api/search` ~800 ms after input (`useEffect([description])` in `home-page.tsx`); pressing Enter/clicking the check fires another. Expect **2** `/api/search` on a typed quick-check, 1 on `/advanced` bulk paste. Neither is `/api/ai-search`.
- `/vs` verdict SSR-vs-DOM: the DOM `#verdict` `textContent` concatenates heading + paragraph with no space (`Which to pickIt depends…`) while tag-stripped SSR has a space, and a naive SSR regex also swallows the adjacent price table. Compare with all whitespace removed and cut the SSR text at `Pricing & 5-year cost of ownership` / 「价格与 5 年持有成本」.
- Several ccTLD-vs-.com English verdicts (de/uk/fr/au-vs-com) share the same 75-char opener; "first 120 chars pairwise distinct" still passes, so also check the shared-opener count across `thin/html/vs/*.en.html` when auditing template phrases.
- MCP `suggest_variants` argument is `name` (not `label`), `tlds` optional, `limit` (cap 48) — a wrong key returns `isError:true` "invalid name…" which is correct validation, not a bug. Read the schema from `tools/list` first.
- Hub filter counts observed on 0358b278: `/tld` "de" → 220, `/vs` "shop" → 20/444 (filter matches descriptions too, so `.beauty vs .salon` appears for "shop").
- Every `?lang=en` page load in the recorded Chrome (including Playwright sweeps on a new tab of the same context) rewrites `domainhunter:lang=en`; restore zh via the header 「中」 button before the storage restore so the UI, not just the key, is proven restored.

## R542–R546 gotchas (quick-check dedupe, form ids, personal shells, 375 px tap targets, share title)
- Quick-check dedupe (R543): `home-page.tsx` keys each request on `${label}|${sorted effective tlds}`; an explicit Enter inside the 800 ms window cancels the timer, so a typed 精确核验 now yields **1** `POST /api/search` (R540's "2×" note is obsolete). Toggling a chip only changes the key if it changes the EFFECTIVE tld set — `.io/.ai/.app/.dev` are already in the default extra set, so toggling them fires 0 requests (correct). To provoke a new key toggle a default-selected chip such as `.cn`. Only 「查更多后缀 +N」 and per-chip 「重新核验 <domain>」 bypass the dedupe (body `{"domains":[…]}` for retry).
- Stable form attributes: `/advanced` `#advanced-roots|prefixes|suffixes|tlds` (`name` = same word, aria zh 「词根，多个用逗号分隔」… en “Roots, comma-separated”…), `#advanced-bulk name=bulk`, `/shortlist` `#shortlist-sync-code name=syncCode` (already visible; the 「导入」 button is disabled while empty — don't click it to expand), hub filters `#prices-filter #tld-filter #guide-filter #vs-filter` all `name=q` with aria == placeholder; `/prices` SSR skeleton also carries `<input id="prices-filter" name="q"`.
- `/shortlist` and `/monitors` are SSR'd by `personalPageShell` with `<html lang>` + title from `?lang=` or `Accept-Language`, always `noindex`, canonical = bare path. Test all four combos with `curl -A Mozilla [-H 'Accept-Language: en|zh']`.
- 375 px tap-target sweep: use CDP emulation 375×812; `document.documentElement.scrollWidth` can read **360** when the emulated viewport shows a classic scrollbar — that is NOT overflow. Confirm with `clientWidth`, `innerWidth` and `body.scrollWidth` (all 375) before reporting. `querySelectorAll` rejects Playwright's `:has-text()`; filter buttons by `innerText` in JS instead. Home chips are `min-w/min-h 44px` on mobile only (desktop stays ≈37–49×24), `/prices` sort buttons 44 → 32 on `sm:`, `/advanced` inputs 44 → 40, `/mcp` Copy `::before` hit zone 48 px.
- Revoked share SPA (`/s/<id>` → 410) sets `document.title` to the EN string “This share has been revoked | DomainHunter” even when `<html lang>=zh-CN` and the body is Chinese (SSR shell title is zh). Known P3 since R546 — don't re-report as new.
- R542 openers: uk/de/au/fr-vs-com en verdicts now start with Nominet/DENIC/auDA/AFNIC sentences, but still share a later sentence (“For a global audience, .com's recognition is irreplaceable…”); grep that phrase when auditing template reuse.
- Lighthouse batch: `--only-categories=seo,accessibility,best-practices,performance` on 10 URLs takes ~4 min; run via `nohup` and poll a log. `/prices` mobile CLS was 0 ×3 on 07d85c8e.

## R548–R553 gotchas (taken/unknown rows, share titles, /vs prose price placeholders)
- Shortlist semantics: only `status==="available"` rows get a price + 去注册; taken → `—` + 「开监控」, unknown/no-status → `—` + 「重新核验」 (`POST /api/check?refresh=1` with only that domain). 「批量去注册（N）」 N = available rows; click opens N `porkbun.com/checkout/search` popups — count them with `context.on("page")` and close, never proceed.
- `/monitors` has **no add form** — monitors are created from a taken row's 「开监控」 (body `{"domain","enabled":true,"status":"taken","webhook"}`) and removed with `enabled:false`. A plan step like "add r553mon.com on /monitors" cannot be executed literally; use a taken row.
- Results-page keyboard registrar test: synthetic `keyboard.press("Enter")` lacks user activation, so `window.open` returns null even for available rows. Click the page for real (computer-use) then ArrowDown/Enter; assert 0 popups on taken rows and 1 on available.
- Share page hydrated `og:locale` can read `en_US` on a zh revoked share (SSR is `zh_CN`) — record as P3, not a title failure; titles are localized since R550.
- R549 /vs prose prices come from `{{price:<tld>:first|renew:usd|cny}}` placeholders rendered by `compare-prices.ts`. Verify: strip `<script>` → `{{` count 0; `main.innerText` `{{` 0; JSON-LD 3 blocks `{{` 0; then compare money mentions (regex `≈?\$\d+(\.\d+)?|≈?[¥￥]\d+|≈?\d+ ?元`) in main-minus-table text against table cells. Static-only TLDs must carry `≈`; `—` appears only when a TLD has neither live nor static price (check `{{price:X:` keys against `/api/prices` + `types.ts`). Also check the **placeholder's TLD matches the sentence subject** — a `{{price:com:…}}` inside a ccTLD sentence renders numerically and passes every `{{`/parity check while being factually wrong (de-vs-com en, R553 P1).
- Comparing DOM `innerText` table cells with SSR: innerText collapses `.cn 参考价` → `.cn参考价`; normalize whitespace before diffing.
- `vs-shared-ngrams` threshold: the repo test uses EN_N=12 (zh 24). At n=8 the four uk/de/au/fr en pages still share 3 short spans — report the threshold you used.
- Full-site `/vs` curl scans with a custom UA count as `pageviews.vs`, not `bots`, in `/api/usage`; explain the delta in the report.
- Every `?lang=en` visit rewrites `domainhunter:lang`; before the final storage diff, restore via the header 「中」 button and re-dump.

## R556–R560 gotchas (/monitors add form, og:locale, font preload, prose word-count measure)
- `/monitors` **now has an add form** (R557): `POST /api/monitor/add {"domain"}` normalizes `https://www.GOOGLE.com/x` → `google.com`; taken → `added:true` + expiry (counts against the 500 global quota), available → `added:false` and the UI refuses + links to a registrar, bad input → 400 `invalid_domain`, `.notatld`/`com.cn` → 400 `unsupported_tld`. `/api/monitor/list` is **POST-only** (GET → 404). Cancel via the UI two-step 「取消监控 → 确认取消？(5s)」 or `enabled:false`; verify `entries=[]` afterwards.
- With text in the add input, Tab first lands on the 「清空输入 ×」 button, then the submit; with an empty input Tab goes straight to submit — not a bug.
- og:locale on `/s/:id` is same-source with html lang/title since R556 (`lib/page-meta.ts`); the R553/R558 "bare zh URL hydrates en_US" did not reproduce on f415f96f. If it appears again, first rule out a stale HTML cache (max-age 600) right after a deploy.
- Font preload warning (`jetbrains-mono … credentials mode does not match`) can linger in the session Chrome for a few minutes after a deploy because the old `index.html` is cached; verify with a fresh headless profile or after `Network.clearBrowserCache` before filing.
- Prose word-count baseline for `/vs` (R540/R546/R560) is **whole-page prose** (`main` minus tables/nav/link chips), not the `#verdict` section alone (uk 767 / de 796 / au 748 / fr 749 at R546; 1030 / 1076 / 960 / 990 at R560). Don't file `#verdict`-only counts as failures.
- Theme state is the `light` class on `<html>` (default dark) when driving the toggle programmatically.

## R562 gotcha (share revoke tokens in committed artifacts)
- Never commit raw share revoke tokens. They appear in `POST /api/share` responses, `DELETE /api/share/:id` request bodies (`{"token":"…"}`) and the `dh:myShares:v1` localStorage dump. Before archiving any request log / storage dump under `docs/audits` or `docs/qa`, replace the value with `<redacted-share-token>`; `apps/web/src/docs-no-share-tokens.test.ts` fails the suite otherwise. GitGuardian flags them as Generic High Entropy Secret on the deploy→main PR (#450), and a live token lets anyone revoke that share.
- Always finish a run by revoking every test share (`DELETE /api/share/:id` with its token) and confirming `GET /api/share/:id` → 410. R545 left two shares live for weeks; R562 revoked them.

## R563–R567 gotchas (?q= exact landing, unknown reasons, recheck endpoint, /vs meta prices)
- `?q=<domain-or-brand-like>` (R564) lands on the exact tab and auto-fires exactly **1** `POST /api/search` (0 `/api/ai-search`, 0 `/api/check`) and scrolls `[data-quick-check]` into view; `?q=<中文寓意>` stays on the AI tab with 0 requests (never click 开始猎取 to "confirm"); `?mode=exact` with no q → exact tab, 0 requests.
- Unknown rows carry `data-unknown-reason` ∈ {rate-limited, recheck-limited, timeout, registry-error, no-rdap, reserved, unparsed, network, generic} — never the raw `detail` (`http-429` etc.). On production `.ai` reliably yields `rate-limited` (registry RDAP 429), so `chaxiang.ai` is a stable unknown fixture. `reserved` rows (no retry button) have not appeared on production yet — only the local mock audit (`docs/qa/audit_browser_r564.py`) covers that branch; mark it 未验证 unless you see one.
- Single-row 重新核验 (`[data-recheck="<domain>"]`, home / `/advanced` / Results) must be exactly one `POST /api/check?refresh=1` with body `{"domains":["<d>"],"refresh":true}` → 200 `application/x-ndjson`, and 0 `/api/search`. **`/api/check` shares the 20/hour per-IP `rl:` bucket with AI search** — budget ≤5 rechecks per run; a 429 renders as `recheck-limited`「重新核验太频繁」 (that is the expected UI, not a bug, but you just burned AI quota for that IP-hour).
- Row selectors: on `/advanced` and Results the recheck button's **direct parent div** is the row; climbing further up (as for home chips) grabs the whole list and produces false FAILs.
- The 375px "any element right>375" overflow check must skip descendants of `overflow-x:auto|scroll` containers (the TLD filter strip); assert `documentElement.scrollWidth==clientWidth==375` instead.
- 开监控 on a taken row also adds the domain to `domainhunter:shortlist` — expected; it is reverted by the storage restore. Read the 「确认取消？」 two-step state only after the countdown finishes, otherwise the "行回开监控" assertion misfires.
- Results page defaults to available-only; click 「全部/All」 before asserting on taken/unknown rows. Synthetic restore via `sessionStorage dh:lastSearch:v1` fires 0 network requests.
- R563 `/vs` meta price parity: compare the price-number sets of `<meta description>`, og/twitter description, Article JSON-LD `description`, FAQPage first answer and the visible first FAQ against the on-page table cells; en uses **rounded** table USD ($35.57→$36), zh matches cells exactly. Second-level TLD pages (`th-vs-vn` → `.co.th`) use relative wording (更便宜 / is cheaper) with no number. Raw SSR bytes still contain `{{price:…}}` **inside the inline `window.__DH_CONTENT__` hydration script** (by design, client re-renders from the same snapshot) — strip `<script>` before counting `{{`; visible text / meta / JSON-LD / hydrated DOM must be 0.
