/**
 * 变体建议规则：心仪名字被注册时，用这些前后缀组合生成变体批量核验。
 * 首页「变体核验」与 MCP suggest_variants 共用，避免两份规则漂移。
 */
export const VARIANT_PREFIXES = ["get", "my", "try", "use"];
export const VARIANT_SUFFIXES = ["app", "hq", "labs", "hub"];
